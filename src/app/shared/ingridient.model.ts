export class Ingridient {
  //   public name: string;
  //   public amount: number;

  // Adding an accessor in front of the variable -> creates the variable
  constructor(public name: string, public amount: number) {
    // this.name = name;
    // this.amount = amount;
  }
}
